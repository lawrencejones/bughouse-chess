<div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
          <div class="navbar-header">
            
            
           <%= link_to image_tag("logo-blue.jpg", size: '50x50'), root_path %>
           
          </div>
          <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav pull-right">
              

                <li class="<%= is_active?(root_path) %>"><%= link_to "Home", root_path %></li>
                <li class="<%= is_active?(top10_path) %>"><%= link_to "Top Players", top10_path %>
                
                <% if signed_in? %>

                  <li class="dropdown">
                    


                    <li class="<%= is_active?(users_path) %>"><%= link_to "Users", users_path %></li>
                    <li id="fat-menu" class="dropdown <%= is_active?(current_user)%><%=is_active?(edit_user_path(current_user)) %>">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                      <%= current_user.name %> <b class="caret"></b>
                    </a>

                   <ul class="dropdown-menu">
                    <li><%= link_to "Profile", current_user, style: 'color:#007aff;' %></li>
                    <li><%= link_to "Settings", edit_user_path(current_user), style: 'color:#007aff;' %></li>
                    <li class="divider"></li>
                    <li><%= link_to "Sign out", signout_path, method: "delete", style: 'color:#007aff;' %></li>
                  </ul>

                  </li>
                  </li>
                <% else %>
                  <li  class="<%= is_active?(signin_path) %>"><%= link_to "Sign in", signin_path %></li>
                <% end %>

            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>